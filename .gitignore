# Composer
/vendor/
composer.lock

# CI build artefacts (release.yml zips the plugin under build/mantia/)
/build/
mantia-*.zip
/mantia.zip

# PHPCS / PHPStan caches
.phpcs-cache
.phpunit.result.cache
phpstan-baseline.neon

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Node (in case we add JS tooling for blocks)
/node_modules/

# wp-env / Docker workspaces
/wordpress/
/tests/fixtures/uploads/*
!/tests/fixtures/uploads/.gitkeep
promptfoo/ux/vars/
promptfoo/ux/node_modules/
promptfoo/ux/interactive_dumps/
promptfoo/ux/package-lock.json
